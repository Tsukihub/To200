const seuil=200;
Var numCase= parseInt(numCase)

function AjoutCase()
{for (var numCase = 1; numCase < seuil; seuil++) {
	document.getElementById("addhere").innerHTML
}

}


	Pour numCase Allant de 1 A SEUIL Au pas de 1
	 	getElementsById.ul.innerhtml("<li>",numCase,"</li>")
	 	
	 	Fpour 
Fin



  for (i = 0; i != a.length; i++)
    resultat[i] = f(a[i]);
  return resultat;
}